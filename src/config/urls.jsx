const domainName = "http://localhost:3000/api";

export const urls = {
    uploadImage: `${domainName}/upload-images`,
    addText: `${domainName}/add-new-text`,
    getEmails: `${domainName}/get-emails`,
    deleteEmail: `${domainName}/delete-email`,
}   